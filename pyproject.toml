[project]
name = "vpnchain-manager"
version = "0.2.0"
description = "VPN Chain Manager v2 skeleton"
requires-python = ">=3.10"

[project.scripts]
vpnchain = "vpnchain.cli:main"
vpnchain-webui = "vpnchain.webui:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
